{
  "buildCommand": "npm run build",
  "outputDirectory": "bitz-frontend/dist",
  "installCommand": "npm install --prefix bitz-frontend",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
